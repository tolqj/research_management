# 科研管理系统 - 完整目录结构

## 📂 项目目录树

```
科研管理系统/
│
├── backend/                          # 后端目录
│   ├── main.py                      # FastAPI主应用入口
│   ├── database.py                  # 数据库配置和连接
│   ├── models.py                    # SQLAlchemy数据模型
│   ├── schemas.py                   # Pydantic数据模式
│   ├── requirements.txt             # Python依赖包列表
│   ├── setup_database.py            # 数据库初始化脚本
│   ├── setup_database.bat           # Windows数据库初始化脚本
│   ├── start_backend.bat            # Windows后端启动脚本
│   │
│   ├── crud/                        # CRUD操作层
│   │   ├── user.py                 # 用户CRUD
│   │   ├── project.py              # 项目CRUD
│   │   ├── paper.py                # 论文CRUD
│   │   ├── fund.py                 # 经费CRUD
│   │   └── achievement.py          # 成果CRUD
│   │
│   ├── routers/                     # 路由模块
│   │   ├── auth.py                 # 认证路由（登录、注册）
│   │   ├── user.py                 # 用户管理路由
│   │   ├── project.py              # 项目管理路由
│   │   ├── paper.py                # 论文管理路由
│   │   ├── fund.py                 # 经费管理路由
│   │   ├── achievement.py          # 成果管理路由
│   │   └── statistics.py           # 统计分析路由
│   │
│   └── utils/                       # 工具函数
│       ├── security.py             # 安全工具（JWT、密码加密）
│       ├── excel.py                # Excel导入导出
│       └── helpers.py              # 通用辅助函数
│
├── frontend/                         # 前端目录
│   ├── index.html                   # HTML入口文件
│   ├── package.json                 # NPM依赖配置
│   ├── vite.config.js               # Vite配置文件
│   ├── start_frontend.bat           # Windows前端启动脚本
│   │
│   └── src/                         # 前端源代码
│       ├── main.js                  # Vue应用入口
│       ├── App.vue                  # 根组件
│       │
│       ├── router/                  # 路由配置
│       │   └── index.js            # 路由定义
│       │
│       ├── store/                   # Pinia状态管理
│       │   └── user.js             # 用户状态
│       │
│       ├── services/                # API服务层
│       │   ├── request.js          # Axios封装
│       │   ├── auth.js             # 认证API
│       │   ├── user.js             # 用户API
│       │   ├── project.js          # 项目API
│       │   ├── paper.js            # 论文API
│       │   ├── fund.js             # 经费API
│       │   ├── achievement.js      # 成果API
│       │   └── statistics.js       # 统计API
│       │
│       ├── views/                   # 页面组件
│       │   ├── login.vue           # 登录页
│       │   ├── layout.vue          # 布局页
│       │   │
│       │   ├── dashboard/          # 工作台
│       │   │   └── index.vue      # 仪表盘
│       │   │
│       │   ├── project/            # 项目管理
│       │   │   ├── index.vue      # 项目列表
│       │   │   └── detail.vue     # 项目详情
│       │   │
│       │   ├── paper/              # 论文管理
│       │   │   └── index.vue      # 论文列表
│       │   │
│       │   ├── fund/               # 经费管理
│       │   │   └── index.vue      # 经费列表
│       │   │
│       │   ├── achievement/        # 成果管理
│       │   │   └── index.vue      # 成果列表
│       │   │
│       │   └── system/             # 系统管理
│       │       ├── user.vue       # 用户管理
│       │       └── profile.vue    # 个人信息
│       │
│       └── components/              # 通用组件（可扩展）
│
├── start_all.bat                     # 一键启动脚本（前后端）
├── build.bat                         # 前端构建脚本
│
├── README.md                         # 项目说明文档
├── 部署指南.md                       # 部署指南
├── 数据库设计文档.md                 # 数据库设计文档
└── 目录结构.md                       # 本文件
```

## 📊 文件统计

### 后端文件
- **核心文件**: 4个（main.py, database.py, models.py, schemas.py）
- **CRUD文件**: 5个
- **路由文件**: 7个
- **工具文件**: 3个
- **脚本文件**: 3个

**总计**: 22个文件

### 前端文件
- **配置文件**: 3个
- **核心文件**: 2个
- **路由/状态**: 2个
- **服务文件**: 8个
- **页面文件**: 10个

**总计**: 25个文件

### 文档和脚本
- **启动脚本**: 3个
- **文档文件**: 4个

**总计**: 7个文件

### 项目总文件数
**54个文件**

## 🎯 关键文件说明

### 后端关键文件

1. **main.py**
   - FastAPI应用入口
   - 路由注册
   - CORS配置
   - 服务启动

2. **database.py**
   - MySQL连接配置
   - SQLAlchemy引擎
   - 会话管理

3. **models.py**
   - 所有数据表定义
   - 字段、索引、关系

4. **schemas.py**
   - 请求/响应数据模型
   - 数据验证规则

5. **utils/security.py**
   - JWT Token生成/验证
   - 密码加密/验证
   - 权限控制

### 前端关键文件

1. **main.js**
   - Vue应用初始化
   - 插件注册
   - Element Plus配置

2. **router/index.js**
   - 路由定义
   - 权限守卫
   - 页面标题

3. **store/user.js**
   - 用户状态管理
   - 登录/登出
   - Token存储

4. **services/request.js**
   - Axios实例配置
   - 请求/响应拦截器
   - 统一错误处理

5. **views/layout.vue**
   - 主布局框架
   - 侧边栏菜单
   - 顶部导航栏

## 🚀 快速定位

### 需要修改配置？
→ `backend/database.py` (数据库)  
→ `backend/utils/security.py` (JWT密钥)  
→ `frontend/vite.config.js` (前端配置)

### 需要添加新功能？
→ `backend/models.py` (新增表)  
→ `backend/routers/` (新增路由)  
→ `frontend/views/` (新增页面)  
→ `frontend/services/` (新增API)

### 需要修改样式？
→ `frontend/src/App.vue` (全局样式)  
→ 各组件的 `<style>` 部分

### 需要查看数据模型？
→ `数据库设计文档.md`

### 需要部署？
→ `部署指南.md`

## 📝 开发规范

- **后端**: 遵循FastAPI最佳实践，使用类型注解
- **前端**: 使用Vue 3 Composition API，组件化开发
- **命名**: 蛇形命名法(Python)、驼峰命名法(JavaScript)
- **注释**: 关键函数必须有文档字符串/注释
- **错误处理**: 统一使用try-catch和HTTP状态码

## 🔄 扩展指南

### 添加新模块
1. 后端：models.py → schemas.py → crud/ → routers/
2. 前端：services/ → views/ → router/index.js
3. 更新文档

### 添加新角色
1. 修改 `models.py` 中的 `UserRole` 枚举
2. 更新 `utils/security.py` 权限控制
3. 调整前端路由守卫

---

**文档更新日期**: 2024-12-02  
**项目版本**: 1.0.0
