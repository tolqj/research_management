<!--pages/profile/index.wxml-->
<view class="container">
  <!-- 用户信息卡片 -->
  <view class="profile-card card">
    <view class="profile-avatar">👤</view>
    <view class="profile-name">{{userInfo.real_name || '用户'}}</view>
    <view class="profile-role">{{userInfo.role === 'admin' ? '系统管理员' : userInfo.role === 'secretary' ? '科研秘书' : '教师'}}</view>
  </view>

  <!-- 用户详细信息 -->
  <view class="info-card card">
    <view class="card-title">个人信息</view>
    <view class="info-item">
      <view class="info-label">用户名</view>
      <view class="info-value">{{userInfo.username}}</view>
    </view>
    <view class="info-item">
      <view class="info-label">姓名</view>
      <view class="info-value">{{userInfo.real_name}}</view>
    </view>
    <view class="info-item" wx:if="{{userInfo.email}}">
      <view class="info-label">邮箱</view>
      <view class="info-value">{{userInfo.email}}</view>
    </view>
    <view class="info-item" wx:if="{{userInfo.phone}}">
      <view class="info-label">电话</view>
      <view class="info-value">{{userInfo.phone}}</view>
    </view>
    <view class="info-item" wx:if="{{userInfo.college}}">
      <view class="info-label">学院</view>
      <view class="info-value">{{userInfo.college}}</view>
    </view>
  </view>

  <!-- 功能列表 -->
  <view class="menu-card card">
    <view class="menu-item" bindtap="handleLogout">
      <view class="menu-label">🚪 退出登录</view>
      <view class="menu-arrow">></view>
    </view>
  </view>
</view>