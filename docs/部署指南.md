# 部署指南

> 完整的生产环境部署指南，包含Windows、Linux和Docker三种部署方式

## 📋 目录

- [环境准备](#环境准备)
- [Windows部署](#windows部署)
- [Linux部署](#linux部署)
- [Docker部署](#docker部署)
- [生产环境配置](#生产环境配置)
- [性能优化](#性能优化)
- [监控与运维](#监控与运维)
- [故障排查](#故障排查)

---

## 环境准备

### 1. 系统要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2核 | 4核+ |
| 内存 | 4GB | 8GB+ |
| 磁盘 | 20GB | 50GB+ |
| 操作系统 | Windows 10/Ubuntu 20.04 | Windows Server 2019/Ubuntu 22.04 |

### 2. 软件依赖

#### Python 3.13+

**Windows**:
```bash
# 下载安装包
https://www.python.org/ftp/python/3.13.0/python-3.13.0-amd64.exe

# 安装时勾选 "Add Python to PATH"
# 验证安装
python --version
pip --version
```

**Linux (Ubuntu/Debian)**:
```bash
sudo apt update
sudo apt install python3.13 python3.13-venv python3-pip -y
python3.13 --version
```

**Linux (CentOS/RHEL)**:
```bash
sudo yum install python3.13 python3-pip -y
python3.13 --version
```

#### Node.js 18.x+

**Windows**:
```bash
# 下载安装包
https://nodejs.org/dist/v18.18.0/node-v18.18.0-x64.msi

# 验证安装
node --version
npm --version
```

**Linux**:
```bash
# 使用NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 或使用nvm（推荐）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18
```

#### MySQL 8.0+

**Windows**:
```bash
# 下载安装包
https://dev.mysql.com/downloads/mysql/

# 安装完成后配置
# 1. 设置root密码
# 2. 启动MySQL服务
net start MySQL80
```

**Linux (Ubuntu/Debian)**:
```bash
sudo apt update
sudo apt install mysql-server -y
sudo systemctl start mysql
sudo systemctl enable mysql

# 安全配置
sudo mysql_secure_installation
```

**Linux (CentOS/RHEL)**:
```bash
sudo yum install mysql-server -y
sudo systemctl start mysqld
sudo systemctl enable mysqld

# 获取临时密码
sudo grep 'temporary password' /var/log/mysqld.log
```

---

## Windows部署

### 1. 准备工作

```bash
# 克隆项目（或解压源码包）
git clone https://github.com/yourusername/research-management-system.git
cd 科研管理系统

# 创建必要的目录
mkdir logs
```

### 2. 配置数据库

**方式一：命令行配置**
```bash
# 登录MySQL
mysql -u root -p

# 执行SQL
CREATE DATABASE research_management_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
SHOW DATABASES;
EXIT;
```

**方式二：使用初始化脚本**
```bash
# 修改 backend\database.py 中的数据库配置
# MYSQL_USER = "root"
# MYSQL_PASSWORD = "你的密码"

cd backend
python setup_database.py
```

### 3. 安装后端依赖

```bash
cd backend

# 创建虚拟环境（推荐）
python -m venv venv
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 验证安装
pip list
```

### 4. 安装前端依赖

```bash
cd frontend

# 安装依赖
npm install

# 如果速度慢，使用国内镜像
npm config set registry https://registry.npmmirror.com
npm install
```

### 5. 启动服务

**方式一：一键启动（推荐）**
```bash
# 返回项目根目录
cd ..

# 双击运行
start_all.bat
```

**方式二：手动启动**
```bash
# 启动后端（窗口1）
cd backend
venv\Scripts\activate
python main.py

# 启动前端（窗口2）
cd frontend
npm run dev
```

### 6. 验证部署

```bash
# 访问前端
http://localhost:5173

# 访问API文档
http://localhost:8000/api/docs

# 健康检查
http://localhost:8000/api/health
```

---

## Linux部署

### 1. 准备工作

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y  # Ubuntu/Debian
# 或
sudo yum update -y  # CentOS/RHEL

# 安装基础工具
sudo apt install git vim curl wget -y

# 克隆项目
cd /opt
sudo git clone https://github.com/yourusername/research-management-system.git
cd research-management-system
```

### 2. 配置数据库

```bash
# 登录MySQL
sudo mysql -u root -p

# 创建数据库和用户
CREATE DATABASE research_management_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'rms_user'@'localhost' IDENTIFIED BY 'StrongPassword@2024';
GRANT ALL PRIVILEGES ON research_management_system.* TO 'rms_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### 3. 配置后端

```bash
cd backend

# 创建虚拟环境
python3.13 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 修改数据库配置
vi database.py
# 修改为：
# MYSQL_USER = "rms_user"
# MYSQL_PASSWORD = "StrongPassword@2024"

# 初始化数据库
python setup_database.py
```

### 4. 配置前端

```bash
cd ../frontend

# 安装依赖
npm install

# 构建生产版本
npm run build

# 构建产物在 dist/ 目录
ls -la dist/
```

### 5. 配置Nginx（推荐）

```bash
# 安装Nginx
sudo apt install nginx -y

# 创建配置文件
sudo vi /etc/nginx/sites-available/rms

# 添加以下配置
server {
    listen 80;
    server_name your-domain.com;  # 修改为您的域名

    # 前端静态文件
    location / {
        root /opt/research-management-system/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 日志配置
    access_log /var/log/nginx/rms_access.log;
    error_log /var/log/nginx/rms_error.log;
}

# 启用配置
sudo ln -s /etc/nginx/sites-available/rms /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 6. 配置Systemd服务

**后端服务**:
```bash
sudo vi /etc/systemd/system/rms-backend.service

# 添加以下内容
[Unit]
Description=Research Management System Backend
After=network.target mysql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/research-management-system/backend
Environment="PATH=/opt/research-management-system/backend/venv/bin"
ExecStart=/opt/research-management-system/backend/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# 启用服务
sudo systemctl daemon-reload
sudo systemctl enable rms-backend
sudo systemctl start rms-backend
sudo systemctl status rms-backend
```

### 7. 验证部署

```bash
# 检查服务状态
sudo systemctl status rms-backend
sudo systemctl status nginx

# 检查端口
sudo netstat -tlnp | grep -E '80|8000'

# 访问系统
curl http://localhost
curl http://localhost/api/health
```

---

## Docker部署

### 1. 安装Docker

**Windows**:
```bash
# 下载Docker Desktop
https://www.docker.com/products/docker-desktop/

# 安装后重启计算机
# 验证安装
docker --version
docker-compose --version
```

**Linux**:
```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# 安装Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 验证安装
docker --version
docker-compose --version
```

### 2. 创建Docker配置文件

**Dockerfile（后端）**:
```bash
# 在 backend/ 目录创建 Dockerfile
cat > backend/Dockerfile << 'EOF'
FROM python:3.13-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    default-libmysqlclient-dev \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 复制应用代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["python", "main.py"]
EOF
```

**Dockerfile（前端）**:
```bash
# 在 frontend/ 目录创建 Dockerfile
cat > frontend/Dockerfile << 'EOF'
FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
EOF
```

**Nginx配置（前端）**:
```bash
cat > frontend/nginx.conf << 'EOF'
server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF
```

**docker-compose.yml**:
```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: rms-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: research_management_system
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  backend:
    build: ./backend
    container_name: rms-backend
    depends_on:
      - mysql
    environment:
      MYSQL_HOST: mysql
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      MYSQL_DATABASE: research_management_system
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    restart: always

  frontend:
    build: ./frontend
    container_name: rms-frontend
    depends_on:
      - backend
    ports:
      - "80:80"
    restart: always

volumes:
  mysql_data:
```

### 3. 启动Docker容器

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 初始化数据库（首次启动）
docker-compose exec backend python setup_database.py
```

### 4. 验证部署

```bash
# 查看容器状态
docker-compose ps

# 访问系统
http://localhost

# 访问API文档
http://localhost/api/docs
```

---

## 生产环境配置

### 1. 安全配置

**修改默认密码**:
```bash
# 首次登录后立即修改admin密码
# 在个人信息页面 -> 修改密码
```

**配置HTTPS**:
```bash
# 使用Let's Encrypt免费证书
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

**配置防火墙**:
```bash
# Ubuntu/Debian
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS/RHEL
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 2. 数据库优化

```sql
-- 创建索引
USE research_management_system;

-- 用户表索引
CREATE INDEX idx_username ON users(username);
CREATE INDEX idx_role ON users(role);

-- 项目表索引
CREATE INDEX idx_pi_id ON projects(pi_id);
CREATE INDEX idx_status ON projects(status);

-- 论文表索引
CREATE INDEX idx_project_id ON papers(project_id);
CREATE INDEX idx_creator_id ON papers(creator_id);

-- 审计日志索引
CREATE INDEX idx_user_id ON operation_logs(user_id);
CREATE INDEX idx_created_at ON operation_logs(created_at);
```

### 3. 备份策略

**自动备份脚本**:
```bash
#!/bin/bash
# 创建 /opt/scripts/backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/opt/backups"
DB_NAME="research_management_system"
DB_USER="root"
DB_PASS="your_password"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u$DB_USER -p$DB_PASS $DB_NAME | gzip > $BACKUP_DIR/db_$DATE.sql.gz

# 删除7天前的备份
find $BACKUP_DIR -name "db_*.sql.gz" -mtime +7 -delete

echo "Backup completed: db_$DATE.sql.gz"
```

**添加到crontab**:
```bash
sudo crontab -e

# 每天凌晨2点自动备份
0 2 * * * /opt/scripts/backup.sh
```

---

## 性能优化

### 1. 后端优化

**使用Gunicorn（生产环境推荐）**:
```bash
# 安装Gunicorn
pip install gunicorn

# 启动（4个worker进程）
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000
```

**配置数据库连接池**:
```python
# backend/database.py
engine = create_engine(
    DATABASE_URL,
    echo=False,  # 生产环境关闭SQL日志
    pool_size=20,  # 连接池大小
    max_overflow=40,  # 最大溢出连接
    pool_pre_ping=True,
    pool_recycle=3600,
)
```

### 2. 前端优化

**启用Gzip压缩（Nginx）**:
```nginx
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
```

**配置缓存**:
```nginx
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 30d;
    add_header Cache-Control "public, immutable";
}
```

### 3. MySQL优化

```sql
-- 查看慢查询
SHOW VARIABLES LIKE 'slow_query%';
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 2;

-- 分析查询
EXPLAIN SELECT * FROM users WHERE username = 'admin';
```

---

## 监控与运维

### 1. 日志管理

**后端日志**:
```python
# 修改 backend/main.py
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('/var/log/rms/backend.log'),
        logging.StreamHandler()
    ]
)
```

**Nginx日志**:
```bash
# 查看访问日志
tail -f /var/log/nginx/rms_access.log

# 查看错误日志
tail -f /var/log/nginx/rms_error.log
```

### 2. 健康监控

**创建监控脚本**:
```bash
#!/bin/bash
# /opt/scripts/health_check.sh

BACKEND_URL="http://localhost:8000/api/health"
FRONTEND_URL="http://localhost"

# 检查后端
if curl -f -s -o /dev/null $BACKEND_URL; then
    echo "✓ Backend is healthy"
else
    echo "✗ Backend is down!"
    sudo systemctl restart rms-backend
fi

# 检查前端
if curl -f -s -o /dev/null $FRONTEND_URL; then
    echo "✓ Frontend is healthy"
else
    echo "✗ Frontend is down!"
    sudo systemctl restart nginx
fi
```

**添加到crontab**:
```bash
*/5 * * * * /opt/scripts/health_check.sh
```

---

## 故障排查

### 1. 后端无法启动

**问题**: Python依赖安装失败

**解决**:
```bash
# 清理缓存重新安装
pip cache purge
pip install -r requirements.txt --force-reinstall
```

**问题**: 数据库连接失败

**解决**:
```bash
# 检查MySQL状态
sudo systemctl status mysql

# 检查配置
python -c "from database import engine; print(engine.url)"

# 测试连接
mysql -h localhost -u root -p research_management_system
```

### 2. 前端无法访问

**问题**: 白屏或404

**解决**:
```bash
# 重新构建
cd frontend
rm -rf node_modules dist
npm install
npm run build

# 检查Nginx配置
sudo nginx -t
sudo systemctl reload nginx
```

### 3. 性能问题

**问题**: 响应慢

**解决**:
```bash
# 检查数据库慢查询
sudo tail -f /var/log/mysql/slow-query.log

# 检查系统资源
top
free -h
df -h

# 查看进程
ps aux | grep python
ps aux | grep nginx
```

---

## 升级与迁移

### 1. 系统升级

```bash
# 备份数据库
mysqldump -u root -p research_management_system > backup.sql

# 拉取最新代码
git pull origin main

# 更新后端依赖
cd backend
pip install -r requirements.txt --upgrade

# 更新前端依赖
cd ../frontend
npm install
npm run build

# 重启服务
sudo systemctl restart rms-backend
sudo systemctl reload nginx
```

### 2. 数据迁移

```bash
# 导出数据
mysqldump -u root -p research_management_system > export.sql

# 导入到新服务器
mysql -u root -p research_management_system < export.sql
```

---

## 安全检查清单

- [ ] 修改所有默认密码
- [ ] 配置HTTPS证书
- [ ] 配置防火墙规则
- [ ] 关闭不必要的端口
- [ ] 定期备份数据库
- [ ] 监控审计日志
- [ ] 定期更新系统补丁
- [ ] 配置日志轮转
- [ ] 设置会话超时
- [ ] 启用IP白名单（可选）

---

**部署完成后，请访问系统并验证所有功能正常！** ✅
