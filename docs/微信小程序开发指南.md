# 科研管理系统 - 微信小程序开发与部署指南

## 📋 目录

1. [项目概述](#项目概述)
2. [快速开始](#快速开始)
3. [完整开发指南](#完整开发指南)
4. [API对接说明](#api对接说明)
5. [生产环境部署](#生产环境部署)
6. [常见问题](#常见问题)

---

## 项目概述

### 功能定位
科研管理系统微信小程序是为**领导层**设计的移动端数据查看工具，主要特点：
- ✅ **只读查看**：不支持数据编辑，确保数据安全
- ✅ **数据总览**：快速查看关键指标
- ✅ **移动优化**：适配小屏幕，简化操作流程
- ✅ **实时同步**：与Web端数据实时同步

### 已实现功能
- ✅ 用户登录认证（JWT Token）
- ✅ 数据总览页面（首页）
- ✅ 项目列表查询（支持分页、筛选）
- ✅ 网络请求封装
- ✅ 工具函数库
- ✅ 全局样式系统

### 待完成功能
- ⏳ 项目详情页面
- ⏳ 论文列表与详情
- ⏳ 经费列表查询
- ⏳ 成果列表查询
- ⏳ 统计图表展示
- ⏳ 个人中心页面

---

## 快速开始

### 步骤1：安装开发工具

下载并安装**微信开发者工具**：
- 官方下载：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
- 选择稳定版（Stable Build）
- 支持Windows、macOS、Linux

### 步骤2：导入项目

1. 打开微信开发者工具
2. 点击"导入项目"
3. 选择项目目录：`E:\qoder\科研管理系统\miniprogram`
4. 填写AppID（可使用测试号）
5. 点击"导入"

### 步骤3：配置后端地址

打开 `app.js`，修改API地址：

```javascript
globalData: {
  userInfo: null,
  token: null,
  // 开发环境：使用本地地址
  baseURL: 'http://localhost:8000/api'
  
  // 或使用局域网IP（用于真机调试）
  // baseURL: 'http://192.168.1.100:8000/api'
}
```

### 步骤4：启动后端服务

```bash
# 进入后端目录
cd backend

# 启动FastAPI服务
python main.py

# 确保看到以下输出：
# INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### 步骤5：编译运行

1. 在微信开发者工具中点击"编译"
2. 在模拟器中查看效果
3. 使用测试账号登录：
   - 管理员：`admin` / `Admin@123`
   - 秘书：`secretary` / `Sec@2024`
   - 教师：`teacher` / `Teacher@123`

---

## 完整开发指南

### 目录结构详解

```
miniprogram/
├── app.js                    # 小程序入口（全局逻辑）
├── app.json                  # 小程序配置（页面注册、tabBar等）
├── app.wxss                  # 全局样式
├── project.config.json       # 项目配置（AppID、编译选项）
├── sitemap.json             # 搜索索引配置
│
├── pages/                    # 页面目录
│   ├── index/               # 首页（数据总览）
│   │   ├── index.js         # 页面逻辑
│   │   ├── index.json       # 页面配置
│   │   ├── index.wxml       # 页面结构
│   │   └── index.wxss       # 页面样式
│   │
│   ├── login/               # 登录页
│   ├── projects/            # 项目模块
│   │   ├── list.js          # 列表页
│   │   └── detail.js        # 详情页
│   │
│   ├── papers/              # 论文模块
│   ├── funds/               # 经费模块
│   ├── achievements/        # 成果模块
│   ├── statistics/          # 统计页面
│   └── profile/             # 个人中心
│
├── utils/                   # 工具类
│   ├── request.js          # 网络请求封装
│   └── util.js             # 通用工具函数
│
└── images/                  # 图片资源
    ├── home.png
    ├── chart.png
    └── user.png
```

### 创建新页面的步骤

#### 1. 在app.json中注册页面

```json
{
  "pages": [
    "pages/index/index",
    "pages/newpage/index"  // 添加新页面
  ]
}
```

#### 2. 创建页面文件

```bash
# 创建页面目录
mkdir pages/newpage

# 创建4个必需文件
touch pages/newpage/index.js     # 页面逻辑
touch pages/newpage/index.json   # 页面配置
touch pages/newpage/index.wxml   # 页面结构
touch pages/newpage/index.wxss   # 页面样式
```

#### 3. 编写页面代码

**index.js**（页面逻辑）：
```javascript
const request = require('../../utils/request')

Page({
  data: {
    list: []
  },
  
  onLoad() {
    this.loadData()
  },
  
  loadData() {
    request.get('/api/endpoint').then(res => {
      this.setData({ list: res })
    })
  }
})
```

**index.wxml**（页面结构）：
```xml
<view class="container">
  <view wx:for="{{list}}" wx:key="id">
    {{item.name}}
  </view>
</view>
```

**index.wxss**（页面样式）：
```css
.container {
  padding: 20rpx;
}
```

**index.json**（页面配置）：
```json
{
  "navigationBarTitleText": "页面标题",
  "enablePullDownRefresh": true
}
```

### 网络请求最佳实践

#### 1. 使用封装的request模块

```javascript
const request = require('../../utils/request')

// GET请求
request.get('/projects/', {
  skip: 0,
  limit: 20,
  status: '执行中'
}).then(res => {
  console.log(res)
}).catch(err => {
  console.error(err)
})

// POST请求
request.post('/projects/', {
  project_name: '新项目',
  pi_id: 1
}).then(res => {
  wx.showToast({ title: '创建成功' })
})
```

#### 2. 处理分页数据

```javascript
Page({
  data: {
    list: [],
    page: 1,
    pageSize: 20,
    finished: false
  },
  
  // 上拉加载更多
  onReachBottom() {
    if (this.data.finished) return
    
    this.setData({ page: this.data.page + 1 })
    this.loadMore()
  },
  
  loadMore() {
    const { page, pageSize } = this.data
    
    request.get('/projects/', {
      skip: (page - 1) * pageSize,
      limit: pageSize
    }).then(res => {
      this.setData({
        list: this.data.list.concat(res),
        finished: res.length < pageSize
      })
    })
  }
})
```

#### 3. 错误处理

```javascript
request.get('/api/endpoint').then(res => {
  // 成功处理
  this.setData({ data: res })
}).catch(err => {
  // 错误处理
  wx.showToast({
    title: err.message || '请求失败',
    icon: 'none'
  })
})
```

### 使用工具函数

```javascript
const util = require('../../utils/util')

Page({
  data: {
    amount: 500000.50,
    date: new Date(),
    status: '执行中'
  },
  
  onLoad() {
    // 格式化金额
    const money = util.formatMoney(this.data.amount)
    console.log(money)  // "500,000.50"
    
    // 格式化日期
    const dateStr = util.formatDate(this.data.date)
    console.log(dateStr)  // "2024-12-02"
    
    // 获取状态类型
    const type = util.getProjectStatusType(this.data.status)
    console.log(type)  // "success"
  }
})
```

---

## API对接说明

### 认证机制

小程序使用**JWT Token认证**：

```
1. 用户登录 → 获取Token
2. 存储Token到本地Storage
3. 每次请求自动携带Token
4. Token过期自动跳转登录
```

**登录流程**：
```javascript
// app.js中的登录方法
login(username, password, callback) {
  wx.request({
    url: `${this.globalData.baseURL}/auth/login`,
    method: 'POST',
    data: { username, password },
    success(res) {
      if (res.statusCode === 200) {
        const token = res.data.access_token
        wx.setStorageSync('token', token)
        callback(true, '登录成功')
      } else {
        callback(false, '登录失败')
      }
    }
  })
}
```

### 接口列表

#### 1. 认证接口

**登录**
```
POST /api/auth/login
Body: { username: "admin", password: "Admin@123" }
Response: { access_token: "eyJ...", token_type: "bearer" }
```

**获取当前用户**
```
GET /api/users/me
Header: Authorization: Bearer <token>
Response: { id: 1, username: "admin", name: "管理员", role: "管理员" }
```

#### 2. 统计接口

**数据总览**
```
GET /api/statistics/overview
Response: {
  project_count: 12,
  paper_count: 20,
  achievement_count: 16,
  total_expense: 500000.00
}
```

#### 3. 项目接口

**项目列表**
```
GET /api/projects/?skip=0&limit=20&status=执行中
Response: [
  {
    id: 1,
    project_name: "深度学习研究",
    pi_name: "张教授",
    status: "执行中",
    budget_total: 500000.00
  }
]
Header: X-Total-Count: 12
```

**项目详情**
```
GET /api/projects/1
Response: {
  id: 1,
  project_name: "深度学习研究",
  pi_name: "张教授",
  pi_id: 1,
  project_type: "国家自然科学基金",
  budget_total: 500000.00,
  start_date: "2024-01-01",
  end_date: "2026-12-31",
  status: "执行中"
}
```

#### 4. 论文接口

**论文列表**
```
GET /api/papers/?skip=0&limit=20
Response: [
  {
    id: 1,
    title: "Deep Learning for Image Classification",
    authors: "Zhang, Li, Wang",
    journal: "IEEE TPAMI",
    jcr_zone: "Q1",
    cas_zone: "一区",
    impact_factor: 24.5,
    publication_date: "2024-01-15"
  }
]
```

#### 5. 经费接口

**经费列表**
```
GET /api/funds/?skip=0&limit=20&project_id=1
Response: [
  {
    id: 1,
    project_id: 1,
    expense_type: "设备费",
    amount: 150000.00,
    expense_date: "2024-03-15",
    handler: "张教授",
    notes: "购买GPU服务器"
  }
]
```

#### 6. 成果接口

**成果列表**
```
GET /api/achievements/?skip=0&limit=20&achievement_type=PATENT
Response: [
  {
    id: 1,
    achievement_type: "PATENT",
    title: "一种图像识别方法",
    owner: "张教授",
    members: "李教授, 王副教授",
    completion_date: "2024-06-01",
    certificate_no: "ZL2024XXXX"
  }
]
```

### 响应头说明

**分页信息**：
- `X-Total-Count`: 总记录数
- 用于计算总页数和显示分页控件

**示例**：
```javascript
request.get('/projects/', { skip: 0, limit: 20 }).then(res => {
  const projects = Array.isArray(res) ? res : []
  const total = parseInt(res.headers?.['x-total-count'] || 0)
  
  console.log(`共${total}条记录，当前页${projects.length}条`)
})
```

---

## 生产环境部署

### 1. 配置HTTPS

小程序要求**生产环境必须使用HTTPS**！

#### 申请SSL证书

**免费选项**：
- Let's Encrypt：https://letsencrypt.org/
- 阿里云/腾讯云免费证书（有效期1年）

**配置Nginx**：
```nginx
server {
    listen 443 ssl;
    server_name api.yourdomain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location /api/ {
        proxy_pass http://localhost:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 2. 配置服务器域名

1. 登录微信公众平台：https://mp.weixin.qq.com/
2. 进入"开发" → "开发管理" → "开发设置"
3. 在"服务器域名"中添加：
   - request合法域名：`https://api.yourdomain.com`
   - 注意：必须是HTTPS，必须备案

### 3. 修改生产环境配置

**app.js**：
```javascript
globalData: {
  userInfo: null,
  token: null,
  // 生产环境API地址
  baseURL: 'https://api.yourdomain.com/api'
}
```

**project.config.json**：
```json
{
  "setting": {
    "urlCheck": true  // 开启域名校验
  }
}
```

### 4. 代码上传与审核

#### 上传代码
1. 在微信开发者工具中点击"上传"
2. 填写版本号和项目备注
3. 上传成功后在公众平台可见

#### 提交审核
1. 登录微信公众平台
2. 进入"版本管理"
3. 点击"提交审核"
4. 填写审核信息：
   - 功能描述
   - 测试账号
   - 使用截图

#### 审核要点
- ✅ 服务器域名必须配置
- ✅ 所有功能正常可用
- ✅ 提供测试账号
- ✅ 无违规内容
- ✅ 隐私政策完整

### 5. 发布上线

审核通过后：
1. 在"版本管理"中点击"发布"
2. 用户可通过小程序码访问
3. 可在"数据分析"中查看使用情况

---

## 常见问题

### 1. 开发环境问题

**Q: 无法连接本地后端？**

A: 检查以下几点：
- 后端服务是否启动？
- baseURL配置是否正确？
- 开发工具中是否开启"不校验合法域名"？
  - 详情 → 本地设置 → 勾选"不校验合法域名、web-view、TLS版本"

**Q: 真机调试无法访问？**

A: 
- 手机和电脑必须在同一局域网
- 使用内网IP而非localhost
- 关闭电脑防火墙或开放8000端口

### 2. 登录认证问题

**Q: 登录后立即跳转登录页？**

A: 
- Token可能未正确保存
- 检查wx.setStorageSync是否成功
- 查看Console输出的错误信息

**Q: 401未授权错误？**

A:
- Token过期或无效
- 检查request.js中是否正确携带Token
- 后端JWT配置是否正确

### 3. 数据显示问题

**Q: 列表数据不显示？**

A:
- 打开调试模式查看Console
- 检查API返回的数据格式
- 确认wxml中的数据绑定正确

**Q: 分页不工作？**

A:
- 检查X-Total-Count响应头是否返回
- 查看finished标志是否正确设置
- 确认onReachBottom方法是否触发

### 4. 部署上线问题

**Q: 域名配置后仍提示不合法？**

A:
- 确认域名已备案
- 确认使用HTTPS协议
- 在公众平台配置后需等待几分钟生效
- 重新编译小程序

**Q: 审核被拒绝？**

A: 常见原因：
- 测试账号无法登录
- 功能描述不清晰
- 缺少隐私政策
- 服务器响应超时
- 按审核意见修改后重新提交

### 5. 性能优化问题

**Q: 加载速度慢？**

A: 优化建议：
- 启用数据缓存
- 压缩图片资源
- 使用分包加载
- 优化网络请求次数

**Q: 图片加载失败？**

A:
- 使用CDN加速
- 图片格式使用WebP
- 添加loading占位图

---

## 开发技巧

### 1. 调试技巧

**Console调试**：
```javascript
console.log('数据:', this.data)
console.error('错误:', err)
console.table(list)  // 表格形式显示数组
```

**断点调试**：
- 在Source面板中打断点
- 查看变量值
- 单步执行

**网络请求调试**：
- Network面板查看请求
- 查看请求头、响应头
- 检查状态码

### 2. 性能优化

**避免频繁setData**：
```javascript
// ❌ 不推荐
for (let i = 0; i < 100; i++) {
  this.setData({ count: i })
}

// ✅ 推荐
this.setData({ count: 100 })
```

**使用节流/防抖**：
```javascript
const util = require('../../utils/util')

Page({
  onSearch: util.debounce(function(e) {
    // 搜索逻辑
  }, 500)
})
```

### 3. 代码规范

**命名规范**：
- 文件名：小写字母+连字符（list-item.js）
- 变量名：驼峰命名（projectList）
- 常量名：大写字母+下划线（BASE_URL）
- 组件名：大写字母开头（MyComponent）

**注释规范**：
```javascript
/**
 * 加载项目列表
 * @param {Number} page 页码
 * @param {String} status 项目状态
 * @returns {Promise} 返回Promise对象
 */
loadProjects(page, status) {
  // 实现代码
}
```

---

## 后续开发计划

### 短期计划（1-2周）

1. ✅ 完成所有页面的wxml和wxss
2. ✅ 实现论文、经费、成果列表页
3. ✅ 添加详情页面
4. ✅ 集成图表组件（ECharts）

### 中期计划（3-4周）

5. 优化用户体验：
   - 添加骨架屏
   - 完善空状态
   - 添加加载动画
6. 数据缓存机制
7. 离线数据查看
8. 消息推送功能

### 长期计划（1-2个月）

9. 权限管理优化
10. 多语言支持
11. 主题切换
12. 数据导出功能
13. 性能监控与优化

---

**文档版本**: v1.0.0  
**最后更新**: 2024-12-02  
**维护团队**: 科研管理系统开发组
